{
  "name": "@evaluation/connected-work-context",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@evaluation/contracts": "workspace:*",
    "@evaluation/database": "workspace:*"
  },
  "devDependencies": {
    "@evaluation/audit": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. packages/connected-work-context/src"
  }
}
