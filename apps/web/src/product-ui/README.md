# Product UI

This directory owns product-specific composition, language, and presentation. It may compose generic
`@evaluation/ui` primitives but must not contain server loaders, domain persistence, authorization
decisions, progress rules, evidence rules, evaluation rules, or provider calls.
