declare const opaque: Record<string, () => unknown>;

let key = "generate";
function execute() {
  return opaque[key]();
}
function invokeNow(callback: () => unknown) {
  return callback();
}

export const result = invokeNow(execute);
key = "health";
