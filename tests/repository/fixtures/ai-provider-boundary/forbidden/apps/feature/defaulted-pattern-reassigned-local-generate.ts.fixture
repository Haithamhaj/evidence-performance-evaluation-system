type Generator = { generate(): string };

declare const providerEnvelope: { client?: Generator };
const local: Generator = { generate: () => "local" };
let client = local;

({ client = local } = providerEnvelope);

export const result = client.generate();
